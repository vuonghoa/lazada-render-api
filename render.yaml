services:
  - type: web
    name: lazada-api
    env: python
    buildCommand: "pip install -r requirements.txt && playwright install"
    startCommand: "python app.py"
    plan: free
    region: singapore
    autoDeploy: false
